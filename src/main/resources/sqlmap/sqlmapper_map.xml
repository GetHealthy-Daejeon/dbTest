<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dbTest.mapper.ExcelMapper">
	<select id="selectYuseong" resultType="map">
		SELECT * 
		from excel.deajeon_facilities
		WHERE gu_code = "유성구"
	</select>
	<select id="dataTest" resultType="map">
		SELECT *
		from dw.board
		where students_id = 23
	</select>
	<select id="facilityList" resultType="map">
	
	</select>
</mapper>